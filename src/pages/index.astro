---
import HudInjectIsland from "../components/HudInjectIsland.jsx";
import { getGitCommitInfo } from "../gitCommitInfo.js";
import "../styles/global.css";

const gitCommitInfo = getGitCommitInfo();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>3D HUD VPK Merger</title>
    <meta
      name="description"
      content="Merge the compiled 3D HUD payload into a locally selected Deadlock addon VPK. Files stay on your machine."
    />
    <meta property="og:title" content="3D HUD VPK Merger" />
    <meta
      property="og:description"
      content="Merge the compiled 3D HUD payload into a locally selected Deadlock addon VPK. Files stay on your machine."
    />
    <script is:inline>
      (() => {
        const key = "3d-hud-theme-mode";
        const stored = localStorage.getItem(key);
        const mode = stored === "light" || stored === "dark" || stored === "system" ? stored : "system";
        const systemDark = matchMedia("(prefers-color-scheme: dark)").matches;
        document.documentElement.dataset.themeMode = mode;
        document.documentElement.dataset.resolvedTheme = mode === "system" ? (systemDark ? "dark" : "light") : mode;
      })();
    </script>
    <link rel="icon" href="data:," />
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>
    <main id="main-content" class="app-shell">
      <HudInjectIsland gitCommitInfo={gitCommitInfo} client:load />
    </main>
  </body>
</html>
